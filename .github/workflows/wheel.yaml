name: wheel
  
on:
  push:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
        python-version: ["3.5", "3.6", "3.7", "3.8", "3.9", "3.10"]
        exclude:
          - python-version: '2.7'
            os: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Checkout submodule
      run: |
        git submodule init
        git submodule update
    - name: Change submodule tag on Windows
      if: ${{ matrix.os == 'windows-latest' }}
      run: |
        git -C src/libslz remote add ciel https://github.com/cielavenir/libslz
        git -C src/libslz fetch ciel
        git -C src/libslz checkout ciel/1.2.0_malloc
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    # - name: "Install Visual C++ for Python 2.7"
    #   if: ${{ matrix.os == 'windows-latest' && matrix.python-version == '2.7' }}
    #   run: |
    #     choco install vcpython27 -f -y
    - name: Install tox and pypa-build
      run: |
        pip install --upgrade pip
        pip install tox build wheel
    - name: Build sdist
      if: ${{ matrix.os == 'macos-latest' && matrix.python-version == '2.7' }}
      run:
        pyproject-build --sdist
    - name: Build slz
      if: ${{ matrix.os == 'windows-latest' }}
      run: |
        gcc -c -DPRECOMPUTE_TABLES=1 -o slz.o -O2 src/libslz/src/slz.c
        as -o chkstk.o src/chkstk.S
    - name: Build wheel
      run: |
        pyproject-build --wheel
    - name: Upload wheel
      uses: actions/upload-artifact@v2
      with:
        name: wheel
        path: dist
